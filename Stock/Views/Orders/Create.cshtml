@model Stock.Models.Order

@{
    ViewData["Title"] = "Добавить заказ";
}

<div class="order-create-container">
    <div class="order-create-card shadow">
        <div class="order-create-header">
            <h1><i class="fas fa-box-open"></i> Добавить новый заказ</h1>
            <p>Заполните форму для добавления нового заказа в систему</p>
        </div>

        <div class="order-create-body">
            <form asp-action="Create" class="order-form">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label asp-for="Name" class="form-label">
                            <i class="fas fa-signature"></i> Название заказа
                        </label>
                        <input asp-for="Name" class="form-control" placeholder="Введите название заказа" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="form-group col-md-6">
                        <label asp-for="Price" class="form-label">
                            <i class="fas fa-dollar-sign"></i> Цена
                        </label>
                        <input asp-for="Price" class="form-control" placeholder="Введите цену" />
                        <span asp-validation-for="Price" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label asp-for="Amount" class="form-label">
                            <i class="fas fa-sort-numeric-up"></i> Количество
                        </label>
                        <input asp-for="Amount" class="form-control" placeholder="Введите количество" />
                        <span asp-validation-for="Amount" class="text-danger"></span>
                    </div>

                    <div class="form-group col-md-6">
                        <label asp-for="CustomerId" class="form-label">
                            <i class="fas fa-user-tag"></i> Клиент
                        </label>
                        <select asp-for="CustomerId" class="form-control" asp-items="ViewBag.CustomerId">
                            <option value="">-- Выберите клиента --</option>
                        </select>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label asp-for="DeliveryId" class="form-label">
                            <i class="fas fa-truck"></i> Способ доставки
                        </label>
                        <select asp-for="DeliveryId" class="form-control" asp-items="ViewBag.DeliveryId">
                            <option value="">-- Выберите способ доставки --</option>
                        </select>
                    </div>

                    <div class="form-group col-md-6">
                        <label asp-for="ProductId" class="form-label">
                            <i class="fas fa-box"></i> Товар
                        </label>
                        <select asp-for="ProductId" class="form-control" asp-items="ViewBag.ProductId">
                            <option value="">-- Выберите товар --</option>
                        </select>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary btn-lg">
                        <i class="fas fa-save"></i> Создать заказ
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
                        <i class="fas fa-arrow-left"></i> Назад к списку
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
    h1 {
        color: white;
    }

    p {
        color: white;
    }

    .order-create-container {
        display: flex;
        justify-content: center;
        padding: 2rem;
    }

    .order-create-card {
        background: white;
        border-radius: 10px;
        width: 100%;
        max-width: 900px;
        overflow: hidden;
    }

    .order-create-header {
        background: linear-gradient(135deg, #2e7d32 0%, #1b5e20 100%);
        color: white;
        padding: 2rem;
    }

        .order-create-header h1 {
            font-size: 1.8rem;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
        }

            .order-create-header h1 i {
                margin-right: 1rem;
            }

    .order-create-body {
        padding: 2rem;
    }

    .order-form .form-label {
        font-weight: 500;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
    }

        .order-form .form-label i {
            margin-right: 0.7rem;
            color: #2e7d32;
            width: 1.2rem;
        }

    .order-form .form-control {
        padding: 0.75rem 1rem;
        border-radius: 5px;
        border: 1px solid #ced4da;
        transition: border-color 0.3s;
    }

        .order-form .form-control:focus {
            border-color: #2e7d32;
            box-shadow: 0 0 0 0.2rem rgba(46, 125, 50, 0.25);
        }

    .form-row {
        display: flex;
        gap: 1rem;
        margin-bottom: 1.5rem;
    }

        .form-row:last-child {
            margin-bottom: 0;
        }

    .form-group {
        flex: 1;
    }

    .form-actions {
        display: flex;
        gap: 1rem;
        margin-top: 2rem;
        padding-top: 1.5rem;
        border-top: 1px solid #eee;
    }

    .btn-primary {
        background-color: #2e7d32;
        border-color: #2e7d32;
        padding: 0.75rem 1.5rem;
    }

        .btn-primary:hover {
            background-color: #1b5e20;
            border-color: #1b5e20;
        }

    .btn-lg {
        font-size: 1.1rem;
    }

    .btn i {
        margin-right: 0.5rem;
    }

    .alert-danger {
        border-left: 4px solid #dc3545;
        padding: 1rem;
        margin-bottom: 1.5rem;
    }

    .text-danger {
        font-size: 0.9rem;
        margin-top: 0.3rem;
    }

    @@media (max-width: 768px) {
        .order-create-container {
            padding: 1rem;
        }

        .order-create-header,
        .order-create-body {
            padding: 1.5rem;
        }

        .form-row {
            flex-direction: column;
        }

        .form-group {
            width: 100%;
        }

        .form-actions {
            flex-direction: column;
        }
    }
</style>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}